{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "vibrate Action",
  "type": "object",
  "properties": {
    "vibrate": {
      "oneOf": [
        {
          "type": "string",
          "enum": ["success", "error", "warning", "light", "medium", "heavy"]
        },
        {
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "enum": ["success", "error", "warning", "light", "medium", "heavy"],
              "description": "Haptic preset. Defaults to success."
            },
            "pattern": {
              "type": "array",
              "items": {
                "type": "number",
                "minimum": 0
              },
              "minItems": 1,
              "description": "Custom vibration sequence in milliseconds using the Web Vibration API convention: even indexes vibrate, odd indexes pause. Overrides type."
            },
            "duration": {
              "type": "number",
              "minimum": 0,
              "description": "Single vibration pulse length in milliseconds. Used when pattern is omitted."
            }
          },
          "additionalProperties": false
        }
      ],
      "description": "Trigger device haptic feedback through navigator.vibrate when supported. String shorthand triggers a preset. Unsupported devices and failures resolve silently and never stop the action chain.",
      "x-examples": [
        "success",
        { "type": "light" },
        { "type": "heavy" },
        { "pattern": [50, 60, 50] },
        { "duration": 200 }
      ]
    }
  },
  "required": ["vibrate"],
  "additionalProperties": false
}
